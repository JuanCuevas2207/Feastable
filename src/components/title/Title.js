var Title = (props) => (
		<div className="Title">
	        <div className="layer">
	            <div className="categoryName">
	            </div>
	        </div>
	        <h1>{props.text}</h1> 
	    </div>
	);

export default Title;
